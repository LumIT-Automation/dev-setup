  ############################################################################################
  # Empty RockyLinux9 vm
  ############################################################################################

  config.vm.define :rocky9 do |rocky9|
    rocky9.vm.provider "virtualbox" do |vb|
      vb.gui = false
      vb.memory = "1024"
      vb.cpus = 1
      vb.customize ["modifyvm", :id, "--natdnshostresolver1", "on"]

      if Vagrant.has_plugin?("vagrant-disksize")
        rocky9.disksize.size = "20GB"
      end
    end

    rocky9.vm.provider "libvirt" do |libvrt|
      libvrt.memory = "1024"
      libvrt.cpus = 1
    end

    # OS.
    rocky9.vm.box = "generic/rocky9"

    # Network.
    rocky9.vm.network :private_network, ip: "10.0.111.206"
    rocky9.vm.hostname = "rocky9"

    # Provision.
    rocky9.vm.provision "shell" do |s|
      s.path = "rocky9/bootstrap.sh"
      s.args = ["--action", "install"]
    end

    # Triggers.
    # Use a script to expand the vdisk with libvirtd.
    #if ! Vagrant.has_plugin?("vagrant-disksize")
    #  rocky9.trigger.after :up do |trigger|
    #    trigger.info = "Expanding disk..."
    #    trigger.run = {inline: "sudo resize_libvirtd.sh rocky9 5 > /tmp/res.log"}
    #  end
    #end

    #rocky9.trigger.after :up do |trigger|
    #  trigger.info = "Expanding filesystem..."
    #  trigger.run_remote = {inline: "diskDevice=`fdisk -l| grep 'Disk /dev' | awk '{print $2}' | sed 's/://'`; printf 'd\nn\n\n\n\n\nw\n' | fdisk $diskDevice; sync; xfs_growfs ${diskDevice}1"}
    #end
  end
